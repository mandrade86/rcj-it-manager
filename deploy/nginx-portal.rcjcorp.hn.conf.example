# LEGACY — no usar Nginx. Usar Apache:
#   deploy/apache-portal.rcjcorp.hn.conf.example
#
# (plantilla Nginx antigua — portal.rcjcorp.hn)
#
# sudo apt install -y nginx
# sudo cp deploy/nginx-portal.rcjcorporacion.com.conf.example /etc/nginx/sites-available/portal.rcjcorporacion.com
# sudo ln -sf /etc/nginx/sites-available/portal.rcjcorporacion.com /etc/nginx/sites-enabled/
# sudo nginx -t && sudo systemctl reload nginx
#
# HTTPS (si el DNS apunta al servidor y el puerto 443 está abierto):
# sudo apt install -y certbot python3-certbot-nginx
# sudo certbot --nginx -d portal.rcjcorporacion.com

server {
    listen 80;
    server_name portal.rcjcorporacion.com;

    client_max_body_size 12M;

    location / {
        proxy_pass http://127.0.0.1:3001;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
    }
}

# Opcional — ambiente Docker de desarrollo (puerto 3002 en el host)
# server {
#     listen 80;
#     server_name portal-dev.rcjcorporacion.com;
#     client_max_body_size 12M;
#     location / {
#         proxy_pass http://127.0.0.1:3002;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
